import {  Routes } from "react-router-dom";

const AppRoutes = () => {
    return (
        <Routes>
            {/* <Route index element={<Dashboard />} /> */}
        </Routes>
    );
};

export default AppRoutes;

